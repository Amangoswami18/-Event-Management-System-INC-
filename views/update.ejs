<!DOCTYPE html>
<html lang="hi">
<head>
  <meta charset="UTF-8">
  <title>Update Event</title>

  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">

  <style>
    body {
      background: #f8f9fa;
      font-family: 'Segoe UI', sans-serif;
      padding: 30px;
    }

    .card {
      max-width: 700px;
      margin: auto;
      padding: 25px;
      border-radius: 12px;
      background-color: #fff;
      box-shadow: 0 6px 18px rgba(0,0,0,0.1);
    }

    .card h2 {
      text-align: center;
      margin-bottom: 25px;
      color: #343a40;
    }

    .form-label {
      font-weight: 500;
    }

    .btn-submit {
      display: block;
      width: 100%;
      margin-top: 15px;
    }
  </style>
</head>
<body>

  <div class="card">
    <h2>Event Update</h2>

    <form method="post" action="/events/update/<%= event.id %>">
      <div class="mb-3">
        <label class="form-label"><b>Event Name:</b></label>
        <input type="text" class="form-control" value="<%= event.name %>" disabled>
      </div>

      <div class="mb-3">
        <label class="form-label">Location:</label>
        <input type="text" name="location" class="form-control" value="<%= event.location %>" required>
      </div>

      <div class="mb-3">
        <label class="form-label">Start Date/Time:</label>
        <input type="datetime-local" name="start_date" class="form-control"
          value="<%= event.start_date ? new Date(event.start_date).toISOString().slice(0,16) : '' %>" required>
      </div>

      <div class="mb-3">
        <label class="form-label">End Date/Time:</label>
        <input type="datetime-local" name="end_date" class="form-control"
          value="<%= event.end_date ? new Date(event.end_date).toISOString().slice(0,16) : '' %>" required>
      </div>

      <button type="submit" class="btn btn-primary btn-submit">Update Event</button>
      <a href="/events/home" class="btn btn-secondary btn-submit mt-2">🔙 Back to Home</a>
    </form>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
