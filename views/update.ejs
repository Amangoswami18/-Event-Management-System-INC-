<!DOCTYPE html>
<html lang="hi">
<head>
  <meta charset="UTF-8">
  <title>Update Event</title>

  <!-- Bootstrap -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <!-- AOS -->
  <link href="https://cdnjs.cloudflare.com/ajax/libs/aos/2.3.4/aos.css" rel="stylesheet">

  <style>
    body {
      background: linear-gradient(135deg, #141e30, #243b55);
      font-family: 'Segoe UI', sans-serif;
      padding-top: 120px;
      color: #ecf0f1;
    }

    /* Header */
    .header {
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      background: linear-gradient(90deg, #0f2027, #203a43, #2c5364);
      box-shadow: 0 6px 20px rgba(0,0,0,0.5);
      z-index: 1000;
      padding: 20px 0;
      text-align: center;
    }

    .page-title {
      margin: 0;
      font-weight: 800;
      font-size: 2.4rem;
      color: #f1c40f;
      text-transform: uppercase;
    }
    .subtitle {
      font-size: 1rem;
      color: #eef0f1;
      margin-top: 5px;
    }

    /* Card */
    .card-custom {
      max-width: 800px;
      margin: auto;
      border-radius: 18px;
      background: rgba(255,255,255,0.05);
      backdrop-filter: blur(12px);
      box-shadow: 0 12px 45px rgba(0,0,0,0.5);
      padding: 35px;
      transition: transform 0.3s ease;
    }
    .card-custom:hover {
      transform: translateY(-5px);
    }

    .card-custom h2 {
      text-align: center;
      margin-bottom: 25px;
      color: #f1c40f;
      font-weight: 700;
      text-shadow: 0 0 10px rgba(241,196,15,0.8);
    }

    .form-label {
      font-weight: 600;
      color: #eff1f2;
    }

    .form-control {
      background: rgba(255,255,255,0.1);
      border: 1px solid rgba(255,255,255,0.3);
      color: black;
    }
    .form-control:focus {
      border-color: #f1c40f;
      box-shadow: 0 0 8px rgba(241,196,15,0.5);
      background: rgba(255,255,255,0.15);
      color: #ffffff;
    }

    .btn-submit {
      background: linear-gradient(135deg, #2980b9, #3498db);
      border: none;
      color: #fff;
      font-weight: 600;
      border-radius: 50px;
      padding: 12px 0;
      transition: all 0.3s ease;
      margin-top: 15px;
    }
    .btn-submit:hover {
      transform: translateY(-3px) scale(1.05);
      box-shadow: 0 0 18px rgba(52,152,219,0.8);
      background: linear-gradient(135deg, #1f618d, #2874a6);
    }
  </style>
</head>
<body>

  <!-- Header -->
  <div class="header" data-aos="fade-down">
    <h2 class="page-title">Update Event</h2>
    <p class="subtitle">आपके इवेंट की जानकारी अपडेट करें</p>
  </div>

  <div class="card-custom" data-aos="zoom-in">
    <h2>Event Update</h2>
    <form method="post" action="/events/update/<%= role %>/<%= event.id %>">
      <div class="mb-3">
        <label class="form-label"><b>कार्यक्रम का नाम:</b></label>
        <input type="text" class="form-control" value="<%= event.name %>" disabled>
      </div>

      <div class="mb-3">
        <label class="form-label">स्थान:</label>
        <input type="text" name="location" class="form-control" value="<%= event.location %>" required>
      </div>

      <div class="mb-3">
        <label class="form-label">प्रारंभ तिथि/समय:</label>
        <input type="datetime-local" name="start_date" class="form-control"
               value="<%= event.start_date ? new Date(event.start_date).toISOString().slice(0,16) : '' %>" required>
      </div>

      <div class="mb-3">
        <label class="form-label">समाप्ति तिथि/समय:</label>
        <input type="datetime-local" name="end_date" class="form-control"
               value="<%= event.end_date ? new Date(event.end_date).toISOString().slice(0,16) : '' %>" required>
      </div>

      <button type="submit" class="btn btn-submit w-100">अपडेट करें</button>
    </form>
  </div>

  <!-- Bootstrap JS -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
  <!-- AOS JS -->
  <script src="https://cdnjs.cloudflare.com/ajax/libs/aos/2.3.4/aos.js"></script>
  <script>
    AOS.init({
      duration: 900,
      easing: 'ease-in-out',
      once: false
    });
  </script>
</body>
</html>
