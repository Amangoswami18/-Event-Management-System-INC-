<!DOCTYPE html>
<html lang="hi">
<head>
  <meta charset="UTF-8">
  <title>Event Details</title>

  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">

  <style>
    body {
      background: #f1f3f5;
      font-family: 'Segoe UI', sans-serif;
      padding: 30px;
    }

    .card {
      max-width: 700px;
      margin: auto;
      border-radius: 12px;
      box-shadow: 0 6px 18px rgba(0,0,0,0.1);
      background-color: #fff;
      padding: 25px;
    }

    .card h2 {
      margin-bottom: 20px;
      font-weight: 600;
      text-align: center;
      color: #343a40;
    }

    .list-group-item {
      border: none;
      padding-left: 0;
      padding-right: 0;
      font-size: 1rem;
    }

    .list-group-item b {
      width: 140px;
      display: inline-block;
      color: #495057;
    }

    .back-btn {
      display: flex;
      justify-content: center;
      margin-top: 20px;
    }

    .btn-back {
      border-radius: 20px;
      padding: 8px 20px;
      font-weight: 500;
    }
  </style>
</head>
<body>

  <div class="card">
    <h2><%= event.name %></h2>

    <ul class="list-group list-group-flush">
      <li class="list-group-item"><b>Description:</b> <%= event.description %></li>

      <li class="list-group-item">
        <b>Start:</b> 
        <%= new Date(event.start_date).toLocaleString("hi-IN", { dateStyle: "long", timeStyle: "short" }) %>
      </li>

      <li class="list-group-item">
        <b>End:</b> 
        <%= new Date(event.end_date).toLocaleString("hi-IN", { dateStyle: "long", timeStyle: "short" }) %>
      </li>

      <li class="list-group-item">
        <b>Issue Date:</b> 
        <%= new Date(event.issue_date).toLocaleDateString("hi-IN", { dateStyle: "long" }) %>
      </li>

      <li class="list-group-item"><b>Location:</b> <%= event.location %></li>
      <li class="list-group-item"><b>Type:</b> <%= event.event_type %></li>
    </ul>

    <div class="back-btn">
      <a href="/events/home" class="btn btn-secondary btn-back">🔙 Back to Home</a>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
