<!DOCTYPE html>
<html lang="hi">
<head>
  <meta charset="UTF-8">
  <title>कार्यक्रम विवरण</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">

  <!-- Bootstrap -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <!-- AOS -->
  <link href="https://cdnjs.cloudflare.com/ajax/libs/aos/2.3.4/aos.css" rel="stylesheet">

  <style>
    body {
      background: #0d1117;
      font-family: 'Segoe UI', sans-serif;
      padding-top: 110px;
      color: #e6edf3;
    }

    /* Header */
    .header {
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      background: linear-gradient(90deg, #141e30, #243b55);
      box-shadow: 0 4px 20px rgba(0,0,0,0.7);
      z-index: 1000;
      padding: 18px 0;
      text-align: center;
    }
    .page-title {
      margin: 0;
      font-weight: 800;
      font-size: 2rem;
      color: #f1c40f;
      text-transform: uppercase;
      text-shadow: 0 0 10px rgba(241,196,15,0.7);
    }
    .subtitle {
      font-size: 0.95rem;
      color: #bdc3c7;
      margin-top: 5px;
    }

    /* Content Table */
    .details-table {
      max-width: 1000px;
      margin: auto;
      background: rgba(255,255,255,0.04);
      border-radius: 12px;
      overflow: hidden;
      box-shadow: 0 8px 35px rgba(0,0,0,0.6);
      animation: fadeIn 0.8s ease-in-out;
    }
    .details-table th {
      background: #1f2937;
      color: #f1c40f;
      font-weight: 700;
      text-align: left;
      padding: 14px 20px;
      font-size: 1rem;
    }
    .details-table td {
      padding: 14px 20px;
      font-size: 1rem;
      border-top: 1px solid rgba(255,255,255,0.05);
      transition: all 0.3s ease;
    }
    .details-table tr:hover td {
      background: rgba(255,255,255,0.06);
      color: #f1c40f;
    }

    /* Footer */
    footer {
      margin-top: 40px;
      padding: 20px;
      text-align: center;
      background: #161b22;
      color: #9ca3af;
      font-size: 0.9rem;
    }

    /* Animations */
    @keyframes fadeIn {
      from {opacity: 0; transform: translateY(20px);}
      to {opacity: 1; transform: translateY(0);}
    }

    /* Responsive */
    @media (max-width: 768px) {
      .page-title { font-size: 1.6rem; }
      .subtitle { font-size: 0.85rem; }
      .details-table th, .details-table td { font-size: 0.95rem; padding: 12px 14px; }
    }
    @media (max-width: 480px) {
      .page-title { font-size: 1.3rem; }
      .subtitle { font-size: 0.75rem; }
      .details-table th, .details-table td { font-size: 0.85rem; padding: 10px 12px; }
    }
  </style>
</head>
<body>

  <!-- Fixed Header -->
  <div class="header" data-aos="fade-down">
    <h2 class="page-title">कार्यक्रम विवरण</h2>
    <p class="subtitle">आपके इवेंट की सम्पूर्ण जानकारी</p>
  </div>

  <!-- Full Page Details -->
  <div class="container mt-4">
    <table class="table details-table" data-aos="zoom-in">
      <tr>
        <th>कार्यक्रम का नाम</th>
        <td><%= event.name %></td>
      </tr>
      <tr>
        <th>विवरण</th>
        <td><%= event.description %></td>
      </tr>
      <tr>
        <th>शुरुआत</th>
        <td><%= new Date(event.start_date).toLocaleString("hi-IN", { dateStyle: "long", timeStyle: "short" }) %></td>
      </tr>
      <tr>
        <th>समाप्ति</th>
        <td><%= new Date(event.end_date).toLocaleString("hi-IN", { dateStyle: "long", timeStyle: "short" }) %></td>
      </tr>
      <tr>
        <th>जारी होने की तिथि</th>
        <td><%= new Date(event.issue_date).toLocaleDateString("hi-IN", { dateStyle: "long" }) %></td>
      </tr>
      <tr>
        <th>स्थान</th>
        <td><%= event.location %></td>
      </tr>
      <tr>
        <th>प्रकार</th>
        <td><%= event.event_type %></td>
      </tr>
    </table>
  </div>

  <!-- Footer -->
  <footer>
    © 2025 आपका संगठन | सर्वाधिकार सुरक्षित
  </footer>

  <!-- Bootstrap JS -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
  <!-- AOS JS -->
  <script src="https://cdnjs.cloudflare.com/ajax/libs/aos/2.3.4/aos.js"></script>
  <script>
    AOS.init({
      duration: 900,
      easing: 'ease-in-out',
      once: true
    });
  </script>
</body>
</html>
