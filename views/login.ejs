<!DOCTYPE html>
<html lang="hi">
<head>
  <meta charset="UTF-8">
  <title>लॉगिन पेज</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">

  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">

  <style>
    body {
      height: 100vh;
      background: linear-gradient(135deg, #121416 0%, #1e2126 100%);
      display: flex;
      justify-content: center;
      align-items: center;
      font-family: 'Segoe UI', sans-serif;
      color: #e0e0e0;
      overflow: hidden;
    }

    .card {
      border-radius: 25px;
      box-shadow: 0 12px 35px rgba(0,0,0,0.7);
      padding: 35px;
      background-color: #1e2126;
      width: 100%;
      max-width: 400px;
      text-align: center;
      animation: slideUp 0.9s ease forwards;
      opacity: 0;
      transition: all 0.3s ease;
    }

    .card h2 {
      color: #ffd700;
      font-weight: 700;
      margin-bottom: 30px;
      letter-spacing: 1px;
    }

    .form-control {
      border-radius: 15px;
      padding: 12px;
      font-size: 1.2rem;
      background: #2c2f33;
      border: 1px solid #444851;
      color: #e0e0e0;
      transition: all 0.3s ease;
      letter-spacing: 5px;
    }

    .form-control::placeholder {
      color: #888;
    }

    .form-control:focus {
      box-shadow: 0 0 12px rgba(0, 170, 255, 0.5);
      border-color: #00aaff;
      background: #383c44;
      color: #ffffff;
      transform: scale(1.02);
    }

    .btn-primary {
      border-radius: 15px;
      font-weight: 600;
      padding: 12px;
      transition: all 0.3s ease;
      background: linear-gradient(135deg, #4CAF50, #81C784);
      border: none;
      color: #fff;
      box-shadow: 0 5px 15px rgba(76,175,80,0.4);
    }

    .btn-primary:hover {
      transform: translateY(-2px) scale(1.03);
      box-shadow: 0 7px 18px rgba(76,175,80,0.6);
    }

    .btn-primary:active {
      transform: scale(0.97);
    }

    @keyframes slideUp {
      from { transform: translateY(50px); opacity: 0; }
      to { transform: translateY(0); opacity: 1; }
    }

    /* Mobile Responsive */
    @media (max-width: 480px) {
      .card { padding: 25px; }
      .card h2 { font-size: 1.8rem; }
      .form-control { font-size: 1rem; padding: 10px; }
      .btn-primary { font-size: 0.9rem; padding: 10px; }
    }
  </style>
</head>
<body>

  <div class="card">
    <h2 >लॉगिन</h2>

    <form method="post" action="/login">
      <div class="mb-4 text-start">
        <label for="code" class="form-label text-white">पासवर्ड (4 अंक)</label>
        <input 
          type="password" 
          id="code" 
          name="code" 
          maxlength="4" 
          pattern="\d{4}" 
          inputmode="numeric"
          class="form-control"
          placeholder="कृपया पासवर्ड दर्ज करें..."
          required>
      </div>

      <button type="submit" class="btn btn-primary w-100 mb-3">लॉगिन करें</button>
    </form>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
