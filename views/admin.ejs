<!DOCTYPE html>
<html lang="hi">
<head>
  <meta charset="UTF-8">
  <title>एडमिन पैनल</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">

  <!-- Bootstrap -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <!-- AOS -->
  <link href="https://cdnjs.cloudflare.com/ajax/libs/aos/2.3.4/aos.css" rel="stylesheet">

  <style>
    /* Body */
    body {
      background: #121416;
      font-family: 'Segoe UI', sans-serif;
      padding: 80px 15px 20px 15px;
      color: #e0e0e0;
    }

    /* Card */
    .card {
      max-width: 1000px;
      margin: auto;
      border-radius: 15px;
      background: #1e2126;
      padding: 25px;
      box-shadow: 0 8px 25px rgba(0,0,0,0.7);
      transition: transform 0.3s ease;
    }
    .card:hover {
      transform: translateY(-3px);
    }

    /* Header */
    h2 {
      text-align: center;
      margin-bottom: 20px;
      font-weight: 700;
      font-size: 1.8rem;
      color: #ffd700;
    }

    /* Add Button */
    .add-btn {
      display: inline-block;
      padding: 8px 20px;
      margin-bottom: 15px;
      font-weight: 600;
      color: #fff;
      border-radius: 25px;
      background: linear-gradient(135deg, #4CAF50, #81C784);
      text-decoration: none;
      transition: all 0.3s ease;
    }
    .add-btn:hover {
      transform: scale(1.05);
      box-shadow: 0 0 12px rgba(76,175,80,0.6);
    }

    /* Table */
    .table-responsive {
      max-height: 500px;
      overflow-x: auto;
      border-radius: 10px;
    }

    table {
      width: 100%;
      border-collapse: separate;
      border-spacing: 0 6px;
      color: #e0e0e0;
    }

    table th {
      background: #2c2f33;
      color: #ffca28;
      font-weight: 600;
      text-align: center;
      font-size: 0.95rem;
    }

    table td {
      background: #1e2126;
      text-align: center;
      vertical-align: middle;
      font-weight: 500;
      font-size: 0.9rem;
      transition: background 0.3s ease;
    }

    table tr:hover td {
      background: #2a2d31;
    }

    /* Event Name */
    .event-name {
      font-weight: 600;
      color: #4fc3f7;
      transition: color 0.3s ease;
    }
    .event-name:hover {
      color: #29b6f6;
    }

    /* Action Buttons */
    a.btn-action {
      margin-right: 5px;
      font-size: 0.85rem;
      border-radius: 25px;
      transition: all 0.3s ease;
    }
    a.btn-info {
      background: #2196F3;
      border: none;
      color: #fff;
    }
    a.btn-info:hover {
      background: #1976D2;
      transform: scale(1.05);
    }
    a.btn-warning {
      background: #FF9800;
      border: none;
      color: #fff;
    }
    a.btn-warning:hover {
      background: #F57C00;
      transform: scale(1.05);
    }

    /* Scrollbar */
    .table-responsive::-webkit-scrollbar {
      width: 10px;
    }
    .table-responsive::-webkit-scrollbar-track {
      background: #1e2126;
    }
    .table-responsive::-webkit-scrollbar-thumb {
      background: #2196F3;
      border-radius: 10px;
    }

    /* Mobile Responsive */
    @media (max-width: 768px) {
      .card {
        padding: 20px;
      }
      h2 {
        font-size: 1.5rem;
      }
      table th, table td {
        font-size: 0.85rem;
      }
      .add-btn {
        padding: 6px 16px;
        font-size: 0.85rem;
      }
      a.btn-action {
        font-size: 0.75rem;
        padding: 4px 8px;
      }
    }

    @media (max-width: 480px) {
      h2 {
        font-size: 1.3rem;
      }
      table th, table td {
        font-size: 0.8rem;
      }
      .add-btn {
        padding: 5px 12px;
        font-size: 0.8rem;
      }
      a.btn-action {
        font-size: 0.7rem;
        padding: 3px 6px;
      }
    }
  </style>
</head>
<body>

  <div class="card" data-aos="fade-up">
    <h2>कार्यक्रम सूची (Admin)</h2>

    <a href="/events/add" class="add-btn">➕ नया कार्यक्रम जोड़ें</a>

    <div class="table-responsive">
      <table class="table table-bordered table-hover">
        <thead>
          <tr>
            <th>क्रम संख्या</th>
            <th>कार्यक्रम का नाम</th>
            <th>क्रिया</th>
          </tr>
        </thead>
        <tbody>
          <% events.forEach((event, index) => { %>
            <tr data-aos="fade-right" data-aos-delay="<%= index*100 %>">
              <td><%= index + 1 %></td>
              <td class="event-name"><%= event.name %></td>
              <td>
                <a href="/events/details/<%= event.id %>" class="btn btn-info btn-sm btn-action">रिपोर्ट देखें</a>
                <a href="/events/update/admin/<%= event.id %>" class="btn btn-warning btn-sm">अपडेट करें</a>
              </td>
            </tr>
          <% }) %>
        </tbody>
      </table>
    </div>
  </div>

  <!-- Bootstrap JS -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
  <!-- AOS JS -->
  <script src="https://cdnjs.cloudflare.com/ajax/libs/aos/2.3.4/aos.js"></script>
  <script>
    AOS.init({
      duration: 900,
      easing: 'ease-in-out',
      once: false
    });
  </script>
</body>
</html>
